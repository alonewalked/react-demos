const initialState = {
    name: ''
};

export function aReducer(state = initialState, action) {
    switch(action.type) {
        case a:
            return Object.assign({}, state);
        case b:
            return Object.assign({}, state);
        default:
            return state;
    }
}
